-- these commands are to set your acces to the DB on windows

set PGHOST=localhost
set PGPORT=5432
set PGUSER=postgres
set PGDATABASE=Northwind
set PGPASSWORD=tu_contraseña_aqui

-- these commands are to set your acces to the DB on mac and linux

export PGHOST=localhost
export PGPORT=5432
export PGUSER=postgres
export PGDATABASE=Northwind
export PGPASSWORD=XXXXX

