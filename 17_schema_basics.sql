-- this way we could acces to information which is stored into a schema

SELECT *
FROM production.product;

-- this query shows everything from the vendor table in purchasing schema

SELECT *
FROM purchasing.vendor;